paths:
    migrations: '%%PHINX_CONFIG_DIR%%/db/migrations'
    seeds: '%%PHINX_CONFIG_DIR%%/db/seeds'

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: 0.0.0.0
        name: cursos
        user: root
        pass: 'securerootpassword'
        port: 3307
        charset: utf8

    development:
        adapter: mysql
        host: 0.0.0.0
        name: cursos
        user: root
        pass: 'securerootpassword'
        port: 3307
        charset: utf8

    testing:
        adapter: mysql
        host: 0.0.0.0
        name: cursos
        user: root
        pass: 'securerootpassword'
        port: 3307
        charset: utf8

version_order: creation
